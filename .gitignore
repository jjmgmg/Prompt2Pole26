# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments (rebuild from requirements.txt — never commit)
.venv/
venv/
# NOTE: do NOT ignore a bare "env/" — it would also match prompt2pole/env/ (source code).

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
